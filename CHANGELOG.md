# 更新日志

本项目的版本变更记录在此文件中。

## [1.0.1] - 2026-08-09

- 插件显示名称调整为“编辑时自动保存”。
- 原生失焦自动保存开启时保持静默，之后若被关闭会再次提醒。
- 新增明确的“不再提示”选择；关闭弹窗不再永久抑制提醒。
- 旧版 `focusSavePromptHandled` 状态不再被视为永久拒绝。

## [1.0.0] - 2026-08-06

- 新增停止输入后的防抖保存，仅保存经过再次校验的当前活动文件。
- 新增可配置延迟，默认 1000 ms，并将有效范围限制在 200–10000 ms。
- 新增过期任务和已切换文档保护，避免计时结束后保存错误文件。
- 修复 Windows ReadOnly 属性未被 HBuilderX editor 标记反映时仍触发保存的问题；在防抖到期后异步检查本地文件可写性。
- 新增首次原生失焦保存授权，以及随时重新检查的命令和人工设置路径。
- 正常保存保持安静，只在保存失败或内部错误时通知。
- 明确隐私边界：无数据收集或上传、无遥测、无广告、无特殊权限。
